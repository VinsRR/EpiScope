# Contributor environment, including every optional backend exercised by the
# full local test suite. The lightweight published install remains `epilens`.
-e .[dev,server,ui,qdrant,mongo,grobid,faiss,providers,local-ml]
