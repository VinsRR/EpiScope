# Precision Miner Integration Tests

Use this folder for tests that exercise the precision miner workflow from
retrieval through parsing.

These tests should usually:
- use the real `PrecisionMiner` workflow
- use fake retrieval and generation inputs
- verify extraction result shape, trace information, and provenance wiring
